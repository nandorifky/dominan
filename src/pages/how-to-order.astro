---
import Layout from '../layouts/Layout.astro';

const pageData = {
  title: "Cara Pemesanan | Transparan & Aman",
  seoDesc: "Panduan lengkap cara memesan jasa di Dominan Web. Mulai dari konsultasi gratis, pembayaran aman, hingga serah terima project.",
  
  // ORDER FLOW
  steps: [
    {
      num: "01",
      title: "Konsultasi & Audit Awal",
      desc: "Hubungi kami via WhatsApp. Ceritakan masalah bisnis Anda. Kami akan melakukan audit singkat (gratis) untuk melihat apakah kami cocok membantu Anda.",
      action: "Gratis",
      color: "bg-white"
    },
    {
      num: "02",
      title: "Proposal & Strategi",
      desc: "Kami kirimkan penawaran resmi (PDF) berisi: Strategi yang akan dilakukan, Timeline pengerjaan, dan Total Biaya. Tidak ada biaya tersembunyi.",
      action: "Transparan",
      color: "bg-dom-yellow"
    },
    {
      num: "03",
      title: "Invoice & DP 50%",
      desc: "Setuju dengan proposal? Kami terbitkan Invoice Tahap 1. Anda cukup bayar DP 50% untuk mengamankan slot waktu tim kami.",
      action: "Secure Payment",
      color: "bg-white"
    },
    {
      num: "04",
      title: "Eksekusi Brutal",
      desc: "Tim mulai bekerja sesuai timeline. Anda akan dimasukkan ke Grup WhatsApp khusus/Trello untuk memantau progress secara real-time.",
      action: "Real-time Update",
      color: "bg-zinc-100"
    },
    {
      num: "05",
      title: "Pelunasan & Serah Terima",
      desc: "Project selesai & dicek bersama (QC). Jika sudah puas, silakan lunasi sisa 50%. Kami serahkan semua akses (Admin, Hosting, Laporan).",
      action: "Full Access",
      color: "bg-green-500",
      textColor: "text-white"
    }
  ],

  // PAYMENT METHODS
  payments: [
    { name: "BCA", number: "123-456-7890", holder: "PT Dominan Digital" },
    { name: "Mandiri", number: "123-000-456-789", holder: "PT Dominan Digital" },
    { name: "QRIS", number: "Scan & Go", holder: "All E-Wallet Support" }
  ],

  faq: [
    { q: "Apakah ada kontrak tertulis?", a: "Ya. Untuk project di atas Rp 5 Juta, kami menyediakan SPK (Surat Perjanjian Kerjasama) bermaterai demi keamanan kedua belah pihak." },
    { q: "Bagaimana jika saya batal di tengah jalan?", a: "Jika pembatalan sepihak dari klien setelah project berjalan, DP tidak dapat dikembalikan karena sudah digunakan untuk resource tim." },
    { q: "Apakah bisa bayar termin/cicil?", a: "Bisa untuk project skala besar (Corporate). Pembayaran bisa dibagi menjadi 3 termin (30% - 40% - 30%) sesuai milestone." }
  ]
};

// Schema Markup (HowTo)
const schemaData = {
  "@context": "https://schema.org",
  "@type": "HowTo",
  "name": "Cara Memesan Jasa Website & SEO Dominan Web",
  "step": pageData.steps.map(step => ({
    "@type": "HowToStep",
    "name": step.title,
    "text": step.desc,
    "position": step.num
  }))
};
---

<Layout 
  title={pageData.title}
  description={pageData.seoDesc}
>
  <script type="application/ld+json" slot="head" set:html={JSON.stringify(schemaData)} />

  {/* =========================================
      1. HERO: NO HASSLE
     ========================================= */}
  <header class="relative bg-zinc-50 border-b-3 border-dom-black pt-32 pb-20 overflow-hidden">
    <div class="absolute left-0 bottom-0 w-32 h-32 bg-dom-yellow rounded-tr-full border-t-3 border-r-3 border-black"></div>
    
    <div class="py-6 container mx-auto px-6 relative z-10 text-center">
        <h1 class="text-5xl md:text-7xl font-bold uppercase leading-none text-dom-black mb-6 tracking-tighter">
            Proses Order <br>
            <span class="bg-dom-black text-white px-4">Tanpa Ribet.</span>
        </h1>
        <p class="text-xl text-gray-600 max-w-2xl mx-auto leading-relaxed">
            Kami memangkas birokrasi. Fokus kami adalah memulai project Anda secepat mungkin dengan administrasi yang rapi dan aman.
        </p>
    </div>
  </header>

  {/* =========================================
      2. THE FLOW (Vertical Timeline)
     ========================================= */}
  <section class="py-24 bg-white border-b-3 border-dom-black">
      <div class="container mx-auto px-6 max-w-4xl">
          
          <div class="space-y-8 relative">
              {/* Vertical Line */}
              <div class="absolute left-4 md:left-8 top-4 bottom-4 w-1 bg-gray-200 z-0"></div>

              {pageData.steps.map((step) => (
                  <div class="relative z-10 flex gap-6 md:gap-10">
                      
                      {/* Number Bubble */}
                      <div class="flex-shrink-0 w-10 h-10 md:w-16 md:h-16 bg-dom-black text-dom-yellow border-3 border-white shadow-lg rounded-full flex items-center justify-center font-bold text-lg md:text-2xl">
                          {step.num}
                      </div>

                      {/* Content Card */}
                      <div class={`flex-grow border-3 border-dom-black p-6 md:p-8 shadow-neo transition-transform hover:-translate-y-1 ${step.color} ${step.textColor || 'text-black'}`}>
                          <div class="flex flex-col md:flex-row md:justify-between md:items-start mb-4 gap-2">
                              <h3 class="text-xl md:text-2xl font-bold uppercase">{step.title}</h3>
                              <span class="inline-block bg-black text-white text-xs font-bold px-3 py-1 uppercase tracking-widest border border-white/20">
                                  {step.action}
                              </span>
                          </div>
                          <p class={`text-lg leading-relaxed ${step.textColor ? 'opacity-90' : 'text-gray-600'}`}>
                              {step.desc}
                          </p>
                      </div>
                  </div>
              ))}
          </div>

      </div>
  </section>

  {/* =========================================
      3. PAYMENT & TERMS
     ========================================= */}
  <section class="py-20 bg-zinc-100 border-b-3 border-dom-black">
      <div class="container mx-auto px-6">
          <div class="grid md:grid-cols-2 gap-16">
              
              {/* Payment Info */}
              <div>
                  <h2 class="text-3xl font-bold uppercase mb-8 border-l-8 border-dom-yellow pl-4">Metode Pembayaran</h2>
                  <div class="bg-white border-3 border-dom-black p-8 shadow-sm">
                      <p class="mb-6 text-gray-600">Semua pembayaran ditransfer ke rekening resmi perusahaan (PT), bukan rekening pribadi, untuk menjamin keamanan dana Anda.</p>
                      <div class="space-y-4">
                          {pageData.payments.map(pay => (
                              <div class="flex items-center justify-between border-b border-dashed border-gray-300 pb-4 last:border-0 last:pb-0">
                                  <div>
                                      <div class="font-bold text-lg">{pay.name}</div>
                                      <div class="text-sm text-gray-500">{pay.holder}</div>
                                  </div>
                                  <div class="font-mono font-bold bg-gray-100 px-3 py-1">{pay.number}</div>
                              </div>
                          ))}
                      </div>
                  </div>
              </div>

              {/* FAQ Small */}
              <div>
                  <h2 class="text-3xl font-bold uppercase mb-8 border-l-8 border-dom-black pl-4">Sering Ditanyakan</h2>
                  <div class="space-y-4">
                      {pageData.faq.map(item => (
                          <details class="bg-white border-2 border-black p-4 cursor-pointer group">
                              <summary class="font-bold flex justify-between items-center list-none">
                                  {item.q}
                                  <span class="group-open:rotate-180 transition-transform">▼</span>
                              </summary>
                              <p class="mt-4 text-gray-600 border-t border-gray-200 pt-4 leading-relaxed">
                                  {item.a}
                              </p>
                          </details>
                      ))}
                  </div>
              </div>

          </div>
      </div>
  </section>

  {/* =========================================
      4. CTA FINAL
     ========================================= */}
  <section class="py-24 bg-white text-center">
      <div class="container mx-auto px-6">
          <h2 class="text-4xl font-bold uppercase mb-8">Sudah Paham Alurnya?</h2>
          <div class="flex flex-col md:flex-row justify-center gap-6">
              <button onclick="document.getElementById('contactModal').classList.remove('hidden')" class="bg-dom-yellow text-black px-10 py-5 font-bold text-xl border-3 border-black shadow-neo hover:shadow-none hover:translate-y-1 transition-all uppercase">
                  Order Sekarang
              </button>
              <a href="/services/seo" class="bg-white text-black px-10 py-5 font-bold text-xl border-3 border-black shadow-neo hover:bg-gray-50 hover:shadow-none hover:translate-y-1 transition-all uppercase">
                  Lihat Layanan Lagi
              </a>
          </div>
      </div>
  </section>

</Layout>